// Config file for TruthGuard Mobile App
// Set API_URL to:
// - "https://your-backend-url.com" (Deployed Production backend, e.g., Render/Railway/Vercel)
// - "http://localhost:5000" (When using iOS Simulator)
// - "http://10.0.2.2:5000" (When using Android Emulator)
// - "http://<YOUR_LOCAL_IP>:5000" (When testing on physical device on same WiFi/Hotspot)
// - "https://<xyz>.ngrok-free.app" (When testing across different networks / mobile data)

export const API_URL = "http://192.168.0.105:5000";
